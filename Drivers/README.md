# Drivers

Purpose: local driver organization for workstation and lab hardware support.

Use vendor folders for downloaded driver packages and document:

- Device model.
- Driver version.
- Source URL.
- Download date.
- Checksum when practical.
- Why the driver is needed.

Driver binaries are ignored by Git. Commit metadata and notes instead.
